# Build
build/
cmake-build-*/

# IDE
.idea/
*.iml
.vscode/

# CMake
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile

# FetchContent cache (large, re-fetched on clean build)
_deps/

# Runtime data (SQLite files are host-volume state, not source)
data/kairos/*.db
data/kairos/*.db-shm
data/kairos/*.db-wal

# Credentials — never commit
kairos.conf

# OS
.DS_Store
Thumbs.db
