# Root .gitignore covers all build, node, IDE, and runtime patterns.
# Only kairos-specific overrides belong here.

# Keep ui-dist tracked — it is the compiled Hades UI served by the kairos binary.
!ui-dist/
